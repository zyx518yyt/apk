# 🍑 粉粉提醒 - APK 打包指南

粉红色主题的每日提醒事项助手。本应用基于 PWA（渐进式 Web 应用）技术开发，可以打包成 Android APK 安装到手机。

---

## 📱 方式一：在线打包（推荐，最简单）

使用微软的 **PWABuilder** 在线工具，无需安装任何开发环境，几分钟即可生成 APK。

### 步骤：

1. **启动本地服务器**（让应用可通过 URL 访问）：
   ```bash
   # 在 reminder-app 目录下
   npx http-server -p 8080 --cors
   ```
   或者使用 Python：
   ```bash
   python -m http.server 8080
   ```

2. **部署到公网**（PWABuilder 需要公网 URL）：
   - 可以使用 GitHub Pages、Vercel、Netlify 等免费托管
   - 将 `reminder-app` 文件夹上传后获得公网 URL

3. **访问 [PWABuilder](https://www.pwabuilder.com/)**：
   - 在输入框粘贴你的应用 URL
   - 点击 **Start**
   - 等待分析完成
   - 点击 **Android** 旁边的 **Generate Package**
   - 填写应用信息（包名建议：com.pinky.reminder）
   - 下载生成的 APK

4. **安装到手机**：
   - 将 APK 传到手机
   - 开启「允许安装未知来源应用」
   - 点击安装

---

## 🛠️ 方式二：本地打包（Bubblewrap）

Google 官方 PWA 转 APK 工具，需要 Java JDK 和 Android SDK。

### 环境准备：

1. **安装 JDK 17**：
   ```bash
   # Windows: 下载安装 OpenJDK
   # https://adoptium.net/
   ```

2. **安装 Bubblewrap CLI**：
   ```bash
   npm install -g @bubblewrap/cli
   ```

3. **初始化项目**：
   ```bash
   bubblewrap init --manifest=https://你的应用地址/manifest.json
   ```

4. **构建 APK**：
   ```bash
   bubblewrap build
   ```

5. 生成的 APK 在 `app-release-signed.apk`

---

## 📦 方式三：Capacitor 打包

使用 Ionic Capacitor 将 Web 应用包装为原生 APK，需要 Android Studio。

### 步骤：

1. **安装 Capacitor**：
   ```bash
   npm init -y
   npm install @capacitor/core @capacitor/cli @capacitor/android
   npx cap init "粉粉提醒" "com.pinky.reminder" --web-dir=reminder-app
   ```

2. **添加 Android 平台**：
   ```bash
   npx cap add android
   npx cap copy
   ```

3. **用 Android Studio 打开**：
   ```bash
   npx cap open android
   ```

4. **在 Android Studio 中 Build → Generate Signed APK**

---

## 🌐 本地预览

无需打包，可直接在浏览器中预览使用：

```bash
# 方法1：Node.js
npx http-server reminder-app -p 8080

# 方法2：Python
cd reminder-app
python -m http.server 8080
```

然后浏览器访问 `http://localhost:8080`

### 手机预览：
1. 电脑和手机连同一 WiFi
2. 查看电脑 IP（如 192.168.1.100）
3. 手机浏览器访问 `http://192.168.1.100:8080`
4. 在浏览器菜单选择「添加到主屏幕」即可像 APP 一样使用

---

## ✨ 应用功能

| 功能 | 说明 |
|------|------|
| 📝 添加提醒 | 自定义事项名称、时间、图标 |
| 🔁 重复设置 | 每天 / 工作日 / 周末 / 仅一次 / 指定星期 |
| ⏰ 提前提醒 | 准时或提前 5/10/15/30/60 分钟 |
| 📋 今日清单 | 按时间排序显示当天提醒 |
| ✅ 完成打卡 | 点击勾选已完成事项 |
| 🔔 通知提醒 | 浏览器系统通知 + 声音 + 震动 |
| 📊 倒计时 | 实时显示下一个提醒倒计时 |
| 📤 数据备份 | 导入导出 JSON 备份文件 |
| 📱 离线使用 | Service Worker 支持离线访问 |

---

## 🎨 设计规格

- **主色调**：粉红色 `#FF6B9D`
- **渐变**：`#FF9EB5 → #FF6B9D → #E85288`
- **字体**：苹方 / 微软雅黑
- **图标**：水蜜桃 🍑
- **风格**：圆角卡片、柔和阴影、少女心

---

## ⚠️ 注意事项

1. **通知权限**：首次使用需在设置中开启通知权限
2. **后台运行**：PWA 在手机后台可能被系统清理，建议添加到主屏幕以获得更好体验
3. **数据存储**：数据保存在本地浏览器中，清除浏览器数据会丢失，请定期导出备份
4. **HTTPS 要求**：Service Worker 和通知功能需要 HTTPS 环境（localhost 除外）

---

 Made with 💕 · 粉粉提醒 v1.0.0
